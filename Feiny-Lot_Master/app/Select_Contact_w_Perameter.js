function loadContact_Perameter_Lookup(targetSelect) {
  //   ZOHO.CREATOR.DATA.getRecords({
  //     app_name: "feiny-app",
  //     report_name: "All_Customers1",
  //   })
  //     .then(function (response) {
  //       if (!targetSelect) return;
  //       targetSelect.innerHTML = `<option value="">Select Contact</option>`;
  //       if (!response.data || response.data.length === 0) return;
  //       response.data.forEach(function (record) {
  //         const option = document.createElement("option");
  //         option.value = record.ID;
  //         option.text = record.Legal_Name;
  //         targetSelect.appendChild(option);
  //       });
  //     })
  //     .catch(function (error) {
  //       console.error("Contacts lookup error:", error);
  //     });
}
